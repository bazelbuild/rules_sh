bcr_test_module:
  module_path: tests/bcr
  matrix:
    platform:
      - rockylinux8
      - debian10
      - ubuntu2004
      - macos
      - macos_arm64
      - windows
    bazel: [7.x, 8.x, 9.x]
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
